config: {{ CONFIG_ID }}
output_template:
  provider: s3
  bucket: {{ sims.RESILIENTSIMS_BUCKET }}
  key_prefix: api_run/{timestamp}_run{run_id}
cloud_credentials:
  s3:
    access_key_id:  {{ s3.S3_ACCESS_KEY }}
    secret_access_key:  {{ s3.S3_SECRET_KEY }}
    endpoint_url:  {{ s3.baseUrl() }}
